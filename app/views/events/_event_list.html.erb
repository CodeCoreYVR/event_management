      <table class="table">
        <% @events.each do |event| %>
        <tr>
          
          <td><%=event.date.strftime("%A, %B %C")%></td>
          <td><a href="#<%=event.id%>"><%=event.title%></a></td>
        </tr>
        <% end %>

      </table>